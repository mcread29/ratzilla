# Local sample video

Place a small, browser-compatible video here as `sample.mp4`.

The example intentionally does not commit or download a media binary. Use a video you created or a clearly redistributable sample. After adding it, rebuild or reload the Trunk page.
